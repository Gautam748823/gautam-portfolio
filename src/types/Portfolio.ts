export interface PortfolioMetadata {
  id: string;
  name: string;
  title: string;
  tagline: string;
  email: string;
  github: string;
  linkedin: string;
  location?: string;
  availability?: string;
}
